module.exports = {
    name: "info",
    description: "Shows information about an alt",
    usage: "<alt-token>",
    execute(args) {
        (async () => {
            utils.getUsername("rhrgyuwvsf");
            log(primary(`// TODO: add actual info`));
        })();
    },
};
